/// The thumb/track shape an [ACDSlideAction] renders by default (ignored
/// when `thumbWidget`/`foregroundBuilder` fully replace the thumb).
enum ACDSlideActionShape {
  /// A rounded-rectangle track with a rounded-rectangle thumb.
  rectangle,

  /// A pill-shaped track with a circular thumb.
  circle,
}
