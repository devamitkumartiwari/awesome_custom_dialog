/// How [ACDCircularPercentIndicator] paints its progress.
enum ACDLoaderFillMode {
  /// A stroked ring/arc — the default.
  ring,

  /// A filled pie slice from the center out to [ACDCircularPercentIndicator.radius].
  pie,
}
